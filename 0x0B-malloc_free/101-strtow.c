#include "main.h"
#include <stdlib.h>

/**
  * strtow - splits a string into words
  * @str: string to be split
  *
  * Return: pointer to an array of split words
  */

char **strtow(char *str)
{
	int i, j, l = 0;
	char **s;

	if (str == NULL || str == "")
		return (NULL);
}
